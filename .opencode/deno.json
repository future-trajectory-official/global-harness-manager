{
  "fmt": {
    "useTabs": false,
    "lineWidth": 100,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false,
    // .opencode/agents（ロール定義）と .opencode/commands（ワークフロー入口）は
    // YAML frontmatter 保持のため fmt 対象外とする。
    "exclude": ["agents", "commands"]
  }
}
